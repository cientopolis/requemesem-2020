### Install

```bash
# Clone the repo
git clone https://github.com/cientopolis/specguru && cd specguru/grupo2/requerimientos

# Setup a virtual environment and install dependencies
python -m venv .venv \
  && source .venv/bin/activate \
  && pip install -r requirements.txt
```
